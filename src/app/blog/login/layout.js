export const metadata = {
  robots: {
    index: false,
    follow: false,
  },
  title: 'Login — Convertic AI',
  description: 'Login to Convertic AI',
};

export default function LoginLayout({ children }) {
  return children;
}
